<?php
if (isset($_GET['id'])) {
    $id = $_GET['id'];

    // You can use the $id variable to perform any operations you need
    // For example, you can use it to fetch data from your database
}
echo $id;
?>